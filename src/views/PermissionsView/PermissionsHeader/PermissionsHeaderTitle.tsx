import * as React from 'react' // tslint:disable-line
import * as cx from 'classnames'
import {$p} from 'graphcool-styles'

export default () =>
  <h1 className={cx($p.f38, $p.fw3, $p.lhSolid)}>Permissions</h1>
